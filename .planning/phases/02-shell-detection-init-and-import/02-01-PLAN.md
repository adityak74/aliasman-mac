---
phase: 2
plan: 02-01
title: Shell Detection, Init, And Import
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/main.rs
  - src/lib.rs
  - src/shell.rs
  - src/import.rs
  - src/store.rs
  - tests/init_shell.rs
autonomous: true
requirements:
  - SHL-01
  - SHL-02
  - SHL-03
  - SHL-04
  - SHL-05
requirements_addressed:
  - SHL-01
  - SHL-02
  - SHL-03
  - SHL-04
  - SHL-05
---

<objective>
Add safe zsh/bash initialization to aliasman: detect the user's shell with safe fallback, preview planned changes, import existing safe aliases without duplication, report skipped risky aliases, add the managed alias source block idempotently, create timestamped backups before config writes, regenerate the managed aliases file, and print shell-specific reload guidance.
</objective>

<requirements>
- **SHL-01:** User can initialize aliasman for zsh or bash with automatic shell detection.
- **SHL-02:** aliasman can import existing aliases from zsh/bash config files without duplicating aliases on repeated runs.
- **SHL-03:** aliasman adds an idempotent managed source block to the detected shell config.
- **SHL-04:** aliasman creates backups before modifying user shell config files.
- **SHL-05:** aliasman prints actionable shell reload hints after any command that changes aliases or shell config.
</requirements>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| User shell config is corrupted by direct writes | high | Reuse atomic write helper and create backups before modifying shell config files. |
| `aliasman init` duplicates source blocks or imported aliases | high | Check managed markers and canonical alias names before appending anything. |
| Tests modify the developer's real shell files | high | All init/import tests use tempdir home fixtures and injectable paths. |
</threat_model>

<truths>
- Detection uses `$SHELL` first, then existing config files; conflicting or missing signals require user choice.
- Import straightforward safe aliases only; protected names and unsupported syntax are skipped and reported.
- Shell config writes create timestamped backups before every write and keep the last 3 aliasman backups.
- Default `aliasman init` behavior is preview-then-confirm before modifying files.
- Reload hints are shell-specific and include the exact config file to source plus the option to open a new terminal.
</truths>

<tasks>
<task id="T1" type="execute">
<title>Add shell detection and config selection</title>
<read_first>
- `src/main.rs`
- `src/lib.rs`
- `src/store.rs`
- `.planning/phases/02-shell-detection-init-and-import/02-RESEARCH.md`
</read_first>
<files>
- `src/lib.rs`
- `src/shell.rs`
- `tests/init_shell.rs`
</files>
<action>
Create `src/shell.rs` with `ShellKind::{Zsh,Bash}` and functions that detect shell kind from a shell path string and select a config file from an injectable home directory. Detection should use `$SHELL`-style shell path first, then existing config files. If signals conflict or no config file exists, return an ambiguous/needs-choice result for the CLI to present to the user. Selection rules: zsh uses `.zshrc`; bash prefers `.bash_profile` when it exists, otherwise `.bashrc`. Export `shell` from `src/lib.rs`.
</action>
<acceptance_criteria>
- `src/shell.rs` contains `pub enum ShellKind`.
- `src/shell.rs` contains `pub fn detect_shell_from_path`.
- `src/shell.rs` contains `pub fn select_shell_config`.
- `tests/init_shell.rs` tests zsh `.zshrc` selection.
- `tests/init_shell.rs` tests bash `.bash_profile` preference.
- `tests/init_shell.rs` tests conflicting signals return an ambiguous/needs-choice result.
- `cargo test shell` exits 0.
</acceptance_criteria>
</task>

<task id="T2" type="execute">
<title>Implement alias import and managed block helpers</title>
<read_first>
- `src/model.rs`
- `src/store.rs`
- `.planning/research/PITFALLS.md`
</read_first>
<files>
- `src/import.rs`
- `src/lib.rs`
- `tests/init_shell.rs`
</files>
<action>
Create `src/import.rs` with helpers to parse simple shell aliases, merge imported aliases into `AliasStore` by name, and insert/remove/check the managed source block. Import only straightforward aliases. Skip protected names and unsupported/complex shell syntax, returning skipped-alias records with reasons for the init preview and summary. Use markers exactly `# >>> aliasman >>>` and `# <<< aliasman <<<`; block body must contain `[ -f "$HOME/.aliases" ] && source "$HOME/.aliases"`. Export `import` from `src/lib.rs`.
</action>
<acceptance_criteria>
- `src/import.rs` contains `pub const MANAGED_BLOCK_START`.
- `src/import.rs` contains `pub const MANAGED_BLOCK_END`.
- `src/import.rs` contains `pub fn parse_alias_lines`.
- `src/import.rs` contains `pub fn merge_imported_aliases`.
- `src/import.rs` contains `pub fn ensure_managed_block`.
- `src/import.rs` contains skipped alias reporting with reason text.
- Tests prove repeated `ensure_managed_block` calls do not duplicate the block.
- Tests prove duplicate alias names import only once.
- Tests prove protected aliases are skipped during import.
</acceptance_criteria>
</task>

<task id="T3" type="execute">
<title>Add init command with backups and reload hints</title>
<read_first>
- `src/main.rs`
- `src/shell.rs`
- `src/import.rs`
- `src/store.rs`
</read_first>
<files>
- `src/main.rs`
- `src/store.rs`
- `tests/init_shell.rs`
</files>
<action>
Add an `init` subcommand to `src/main.rs`. The command should detect shell/config paths, build and display a preview, require confirmation before writing, load/import safe aliases, report skipped aliases, write canonical metadata, regenerate managed aliases, create a timestamped backup before shell config modification, prune older aliasman backups beyond the latest 3, atomically write the updated config, and print a shell-specific reload hint such as `source ~/.zshrc` or `source ~/.bash_profile` plus a note that opening a new terminal also works. Keep internals path-injectable so tests use tempdirs.
</action>
<acceptance_criteria>
- `src/main.rs` contains `Init`.
- `src/main.rs` contains preview/confirmation behavior before writing.
- `src/main.rs` contains `source ~/.zshrc` or shell-specific reload hint generation.
- `src/store.rs` contains a reusable backup helper or equivalent backup write function.
- `tests/init_shell.rs` verifies backup creation before config modification.
- `tests/init_shell.rs` verifies older aliasman backups beyond the latest 3 are pruned.
- `tests/init_shell.rs` verifies init output/reload text contains shell-specific source guidance.
- `cargo test init_shell` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `cargo fmt --check`, `cargo test`, and `cargo run -- init` against tempdir-backed tests or test helpers only. Do not run init against the real home directory during automated verification.
</verification>

<success_criteria>
- `aliasman init` exists and is test-covered.
- zsh/bash shell detection and config selection work.
- Existing aliases import idempotently.
- Managed source block insertion is idempotent.
- Shell config writes create backups.
- Mutation output includes reload guidance.
</success_criteria>

<must_haves>
- SHL-01 through SHL-05 are all covered.
- No test touches real shell config files.
- Repeated init behavior is idempotent.
- Phase 2 decisions D-01 through D-17 from `02-CONTEXT.md` are represented in implementation and tests.
</must_haves>
