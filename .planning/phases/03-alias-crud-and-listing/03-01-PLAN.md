---
phase: 3
plan: 03-01
title: Alias CRUD And Listing
type: execute
wave: 1
depends_on:
  - 01-01
  - 02-01
files_modified:
  - src/main.rs
  - src/store.rs
  - src/validation.rs
  - tests/crud_cli.rs
autonomous: true
requirements:
  - ALS-01
  - ALS-02
  - ALS-03
  - ALS-04
  - ALS-05
requirements_addressed:
  - ALS-01
  - ALS-02
  - ALS-03
  - ALS-04
  - ALS-05
---

<objective>
Implement the first complete user-facing alias workflow: add, update, delete, and list aliases through named CLI flags with validation, canonical metadata persistence, regenerated managed alias output, reload hints, and actionable errors.
</objective>

<requirements>
- **ALS-01:** User can add an alias with named flags.
- **ALS-02:** User can update an existing alias with named flags.
- **ALS-03:** User can delete an alias by name.
- **ALS-04:** User can list aliases in a readable CLI table.
- **ALS-05:** User can see useful errors for duplicate aliases, missing aliases, invalid flags, and write failures.
</requirements>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| CRUD command silently overwrites an existing alias | high | `add` must reject duplicates; `update` is the explicit replacement path. |
| Protected command alias is created accidentally | medium | Reuse `validate_alias_name_for_write` and require `--force`. |
| User sees success but current shell cannot use alias | medium | Every mutation prints a reload hint containing `source ~/.aliases`. |
</threat_model>

<tasks>
<task id="T1" type="execute">
<title>Add CRUD subcommands and store mutation helpers</title>
<read_first>
- `src/main.rs`
- `src/store.rs`
- `src/validation.rs`
- `.planning/phases/03-alias-crud-and-listing/03-RESEARCH.md`
</read_first>
<files>
- `src/main.rs`
- `src/store.rs`
</files>
<action>
Add `add`, `update`, `delete`, and `list` clap subcommands. `add` and `update` accept `--name <name> --command <command>`, optional `--description <text>`, optional repeated `--tag <tag>`, and `--force`. `delete` accepts `--name <name>`. Implement store helpers for add/update/delete by alias name. Mutations must save canonical metadata and regenerate managed aliases.
</action>
<acceptance_criteria>
- `src/main.rs` contains `Add`.
- `src/main.rs` contains `Update`.
- `src/main.rs` contains `Delete`.
- `src/main.rs` contains `List`.
- `src/main.rs` contains `--force` or a `force` field.
- `src/store.rs` contains add/update/delete helper functions or methods.
- `cargo test crud` exits 0 after T3 tests are added.
</acceptance_criteria>
</task>

<task id="T2" type="execute">
<title>Implement list output and actionable errors</title>
<read_first>
- `src/main.rs`
- `src/store.rs`
</read_first>
<files>
- `src/main.rs`
- `src/store.rs`
</files>
<action>
Implement `aliasman list` as a readable CLI table containing at least alias name and command. Add error paths for duplicate alias, missing alias, invalid alias name, protected name without force, and write failures. Error text should include the alias name and the action that failed.
</action>
<acceptance_criteria>
- `aliasman list` output includes headers `Name` and `Command`.
- Duplicate add error contains `already exists`.
- Missing update/delete error contains `not found`.
- Protected-name error contains `--force`.
- Invalid-name error contains `invalid alias name`.
</acceptance_criteria>
</task>

<task id="T3" type="execute">
<title>Add CRUD CLI integration tests</title>
<read_first>
- `src/main.rs`
- `src/store.rs`
- `tests/init_shell.rs`
</read_first>
<files>
- `tests/crud_cli.rs`
</files>
<action>
Create tempdir-backed CLI/store integration tests for add, duplicate add, update, delete, list, protected-name force handling, and reload hints. Tests must not use the real home directory. Use helper env vars or explicit test paths if the CLI supports them; otherwise test library mutation helpers directly and add minimal CLI parsing assertions.
</action>
<acceptance_criteria>
- `tests/crud_cli.rs` contains `adds_alias_with_named_flags`.
- `tests/crud_cli.rs` contains `duplicate_add_fails`.
- `tests/crud_cli.rs` contains `updates_existing_alias`.
- `tests/crud_cli.rs` contains `deletes_existing_alias`.
- `tests/crud_cli.rs` contains `lists_aliases_in_table`.
- `tests/crud_cli.rs` contains `protected_alias_requires_force`.
- `cargo test` exits 0.
- `cargo fmt --check` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `cargo fmt --check`, `cargo test`, `cargo run -- add --name gs --command "git status"` with a tempdir-backed test configuration only, and `cargo run -- list` with the same tempdir-backed configuration.
</verification>

<success_criteria>
- Add/update/delete/list commands exist.
- Mutations update canonical metadata and regenerated aliases.
- User-facing errors are actionable.
- Reload hints print after mutations.
</success_criteria>

<must_haves>
- ALS-01 through ALS-05 are all covered.
- No CRUD test writes to real home files.
- Protected names require explicit force handling.
</must_haves>

