---
phase: 4
plan: 04-01
title: History Stats And Suggestions
type: execute
wave: 1
depends_on:
  - 01-01
  - 03-01
files_modified:
  - src/main.rs
  - src/lib.rs
  - src/history.rs
  - src/store.rs
  - tests/history_cli.rs
autonomous: true
requirements:
  - HST-01
  - HST-02
  - HST-03
  - HST-04
  - HST-05
requirements_addressed:
  - HST-01
  - HST-02
  - HST-03
  - HST-04
  - HST-05
---

<objective>
Add shell-history analytics and safe alias suggestions. Users can inspect frequent commands and view suggested aliases, while alias creation from history remains explicit and validated.
</objective>

<requirements>
- **HST-01:** User can view command frequency statistics from zsh or bash history.
- **HST-02:** aliasman correctly parses zsh extended history format.
- **HST-03:** aliasman suggests short aliases for frequent long commands.
- **HST-04:** User must explicitly approve a history-derived suggestion before it becomes an alias.
- **HST-05:** aliasman flags risky history-derived commands that contain command substitution or other shell metacharacters.
</requirements>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| History suggestion auto-creates unsafe alias | high | `suggest` is display-only unless an explicit accept/apply path is provided and routed through CRUD validation. |
| zsh extended history timestamps are treated as commands | medium | Dedicated zsh extended parser with fixtures. |
| Large history file causes slow or memory-heavy command | medium | Use streaming or bounded line processing. |
</threat_model>

<tasks>
<task id="T1" type="execute">
<title>Add history parsing and aggregation</title>
<read_first>
- `src/lib.rs`
- `.planning/phases/04-history-stats-and-suggestions/04-RESEARCH.md`
- `.planning/research/PITFALLS.md`
</read_first>
<files>
- `src/lib.rs`
- `src/history.rs`
- `tests/history_cli.rs`
</files>
<action>
Create `src/history.rs` with parsers for zsh extended lines (`: <epoch>:<duration>;<command>`), plain zsh/bash command lines, and bash timestamp markers (`#<epoch>`). Add command frequency aggregation that returns commands sorted by descending count, then command text for deterministic output. Export `history` from `src/lib.rs`.
</action>
<acceptance_criteria>
- `src/history.rs` contains `parse_zsh_extended`.
- `src/history.rs` contains `command_frequencies`.
- `src/history.rs` contains `: <epoch>:<duration>;` only in comments or tests, not as parsed output.
- Tests prove `: 1715300000:0;git status` parses to `git status`.
- Tests prove bash `#1715300000` is not counted as a command.
- `cargo test history` exits 0.
</acceptance_criteria>
</task>

<task id="T2" type="execute">
<title>Add stats and suggest commands</title>
<read_first>
- `src/main.rs`
- `src/history.rs`
- `src/store.rs`
</read_first>
<files>
- `src/main.rs`
- `src/history.rs`
</files>
<action>
Add `stats` and `suggest` subcommands. `stats` reads a history file path argument or detected/default path and prints command/count rows. `suggest` prints suggested alias names and commands based on frequency and command length. Suggestions must be display-only unless the executor implements an explicit `--accept <name>` path that calls existing add/store validation.
</action>
<acceptance_criteria>
- `src/main.rs` contains `Stats`.
- `src/main.rs` contains `Suggest`.
- `stats` output includes `Command` and `Count`.
- `suggest` output includes `Suggested` or `Alias`.
- `suggest` without explicit accept/apply does not modify the alias store.
</acceptance_criteria>
</task>

<task id="T3" type="execute">
<title>Add risky-command flags and history tests</title>
<read_first>
- `src/history.rs`
- `src/validation.rs`
- `tests/crud_cli.rs`
</read_first>
<files>
- `src/history.rs`
- `tests/history_cli.rs`
</files>
<action>
Add risky command detection for `$(`, backticks, `<(`, `|`, `;`, `&&`, and `||`. Suggestions containing those patterns should be marked with a review warning in output. Add tests for stats ranking, suggestion ranking, risky command flags, and display-only suggestion behavior.
</action>
<acceptance_criteria>
- `src/history.rs` contains `is_risky_history_command`.
- Tests include `flags_command_substitution_as_risky`.
- Tests include `flags_shell_chaining_as_risky`.
- Tests include `suggestions_are_display_only_by_default`.
- `cargo test` exits 0.
- `cargo fmt --check` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `cargo fmt --check`, `cargo test`, `cargo run -- stats --history-file <fixture>`, and `cargo run -- suggest --history-file <fixture>` using fixture/tempdir history files.
</verification>

<success_criteria>
- zsh extended history is parsed correctly.
- stats show frequency analytics.
- suggestions are useful but not automatically applied.
- risky suggestions are flagged.
</success_criteria>

<must_haves>
- HST-01 through HST-05 are all covered.
- History-derived aliases require explicit user approval.
- Risky command patterns are visible before acceptance.
</must_haves>

