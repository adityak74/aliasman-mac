---
phase: 09-pack-install-with-safety
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/pack_installer.rs]
autonomous: true
requirements: [INST-07, INST-08]
user_setup: []

must_haves:
  truths:
      - "After a successful install, regenerate_aliases is called (verified by .aliases file being updated)"
      - "After a successful install, refresh_index is called (verified by semantic index being rebuilt)"
      - "After a successful install, the pack appears in the registry (PackRegistry::load shows the pack)"
      - "Post-install hooks (regenerate_aliases, refresh_index, print_reload_hint) are triggered from the CLI handler"
  artifacts:
      - path: "src/pack_installer.rs"
      provides: "Integration tests for install flow: registry registration, collision skipping, post-install hooks"
      contains: "mod install_integration_tests"
  key_links:
      - from: "src/pack_installer.rs tests"
      to: "src/pack_registry.rs"
      via: "PackRegistry::load() after install_pack()"
      pattern: "PackRegistry::load"
---

<objective>
Add integration tests for the pack install flow covering registry registration, collision behavior, and post-install hook triggers.

Purpose: Verify that install_pack() correctly registers packs in the registry, skips collisions by default, and that the CLI handler triggers post-install hooks.

Output: Extended test module in `src/pack_installer.rs` with integration tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/pack_installer.rs
@src/pack_manager.rs
@src/pack_registry.rs
@src/store.rs
@src/model.rs
</context>

<interfaces>
<!-- Key types for test construction -->

From src/pack_installer.rs:
```rust
pub fn install_pack(
    export: PackExport,
    force: bool,
    user_store: &AliasStore,
    source: String,
) -> Result<InstallResult, InstallError>

pub struct InstallResult {
    pub pack_name: String,
    pub installed_count: usize,
    pub skipped_collisions: Vec<String>,
    pub had_warnings: bool,
}
```

From src/pack_registry.rs:
```rust
pub struct PackRegistry {
    pub packs: Vec<RegistryEntry>,
}
impl PackRegistry {
    pub fn load() -> Result<Self, RegistryError>
    pub fn get_pack(&self, name: &str) -> Option<&RegistryEntry>
    pub fn unregister_pack(&mut self, name: &str) -> bool
    pub fn save(&self) -> Result<(), RegistryError>
}
```

From src/pack_manager.rs (test helpers):
```rust
pub fn get_pack_dir(pack_name: &str) -> Result<PathBuf, PackError>
pub fn pack_exists(pack_name: &str) -> bool
pub fn load_pack_aliases(pack_name: &str) -> Result<Vec<AliasRecord>, PackError>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
    <name>Task 1: Add install integration tests — registry, collisions, post-install</name>
    <files>src/pack_installer.rs</files>
    <behavior>
      - Test 1: install_pack with clean store registers pack in registry (PackRegistry::load has entry with correct name/version/source/alias_count)
      - Test 2: install_pack with colliding user aliases (force=false) skips collisions (InstallResult.skipped_collisions contains the names, installed_count excludes them)
      - Test 3: install_pack with colliding user aliases (force=true) overrides collisions (InstallResult.skipped_collisions is empty, installed_count includes all)
      - Test 4: install_pack with safety warnings (force=false) returns InstallError::SafetyViolation
      - Test 5: install_pack with safety warnings (force=true) proceeds and returns InstallResult with had_warnings=true
      - Test 6: install_pack with empty alias name returns InstallError::InvalidFormat
      - Test 7: install_pack with empty command returns InstallError::InvalidFormat
    </behavior>
    <read_first>
      - src/pack_installer.rs — Full file to see existing test module (lines 361-451), helper functions (make_alias, make_user_alias), and install_pack signature (lines 265-351)
      - src/pack_registry.rs — PackRegistry::load(), get_pack(), unregister_pack(), save() for test cleanup
      - src/pack_manager.rs — get_pack_dir(), pack_exists(), for test cleanup
      - src/model.rs — AliasRecord, AliasSource definitions for test construction
    </read_first>
    <action>
     Add a new test module `#[cfg(test)] mod install_integration_tests` inside src/pack_installer.rs (after the existing tests module) with the following tests:

      1) `install_registers_pack_in_registry`:
        - Create a PackExport with a unique pack name (use process ID or timestamp for uniqueness)
        - Create an empty AliasStore
        - Call install_pack(export, false, &store, "test".to_string())
        - Verify: result.is_ok(), InstallResult.installed_count == alias count
        - Verify: PackRegistry::load() has entry with matching name, version, alias_count
        - Cleanup: unregister pack, delete pack directory

      2) `install_skips_collisions_without_force`:
        - Create a PackExport with alias "gs" = "git status -s"
        - Create AliasStore with user alias "gs" = "git status" (source = AliasSource::User)
        - Call install_pack(export, false, &store, "test".to_string())
        - Verify: result.ok(), InstallResult.skipped_collisions contains "gs"
        - Verify: InstallResult.installed_count == 0 (all skipped)
        - Cleanup: delete pack directory

      3) `install_overrides_collisions_with_force`:
        - Same setup as test 2
        - Call install_pack(export, true, &store, "test".to_string())
        - Verify: result.ok(), InstallResult.skipped_collisions is empty
        - Verify: InstallResult.installed_count == 1
        - Cleanup: unregister pack, delete pack directory

      4) `install_blocks_safety_warnings_without_force`:
        - Create PackExport with alias containing "$(whoami)" (command substitution)
        - Call install_pack(export, false, &store, "test".to_string())
        - Verify: result.err(), error is InstallError::SafetyViolation
        - Cleanup: delete pack directory

      5) `install_proceeds_with_safety_warnings_and_force`:
        - Same setup as test 4
        - Call install_pack(export, true, &store, "test".to_string())
        - Verify: result.ok(), InstallResult.had_warnings == true
        - Cleanup: unregister pack, delete pack directory

      6) `install_rejects_empty_alias_name`:
        - Create PackExport with alias name = ""
        - Call install_pack
        - Verify: result.err(), error is InstallError::InvalidFormat mentioning "empty name"

      7) `install_rejects_empty_command`:
        - Create PackExport with alias command = ""
        - Call install_pack
        - Verify: result.err(), error is InstallError::InvalidFormat mentioning "empty command"

     Use helper functions from existing test module (make_alias, make_user_alias) or create new ones. Use unique pack names with process ID to avoid conflicts. Clean up pack directories after each test.
    </action>
    <verify>
      <automated>cargo test --lib install_integration_tests -- 2>&1 | grep -c "test result: ok" == 1</automated>
    </verify>
    <acceptance_criteria>
      - `cargo test --lib install_integration_tests` runs all 7 tests with zero failures
      - Test output shows "test result: ok N passed; 0 failed" where N >= 7
      - Each test cleans up after itself (no leftover pack directories or registry entries)
      - Tests cover: registry registration, collision skip (force=false), collision override (force=true), safety block (force=false), safety override (force=true), empty name rejection, empty command rejection
    </acceptance_criteria>
    <done>
    cargo test --lib install_integration_tests passes all 7 tests with 0 failures
    </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test data → install_pack() | Test-created PackExport objects exercise safety scanner; no real user data at risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-06 | Spoofing | Test PackExport objects | accept | Tests run in isolated temp directories; no production data affected |
</threat_model>

<verification>
1. `cargo test --lib install_integration_tests` — all tests pass
2. `cargo test --lib pack_installer` — all tests (existing + new) pass together
</verification>

<success_criteria>
- 7 integration tests for install flow pass (registry, collisions, safety, validation)
- All existing pack_installer tests still pass alongside new tests
- Tests clean up after themselves (no persistent side effects)
</success_criteria>

<output>
Create `.planning/phases/09-pack-install-with-safety/09-02-SUMMARY.md` when done
</output>
